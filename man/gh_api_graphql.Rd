% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gh_api.R
\name{gh_api_graphql}
\alias{gh_api_graphql}
\title{Make a GitHub GraphQL API request}
\usage{
gh_api_graphql(query, ..., variables = list(), repo = NULL)
}
\arguments{
\item{query}{A GraphQL query string.}

\item{...}{Ignored.}

\item{variables}{A named list of GraphQL variables.  Passed as the
\code{variables} key in the JSON request body.}

\item{repo}{Repository in \code{OWNER/REPO} format.  Controls what \code{{owner}}
and \code{{repo}} placeholders expand to.  If \code{NULL} uses the current repository.}
}
\value{
A named list representing the \code{data} element of the GraphQL response.
}
\description{
\code{gh_api_graphql()} makes an authenticated GraphQL request to the GitHub API.
}
\examples{
\dontrun{
  # requires `gh` installed and authenticated and working directory in Github repository
  gh_api_graphql('{ repository(owner: "trevorld", name: "ghcli") { hasSponsorshipsEnabled } }')

  # with GraphQL variables
  gh_api_graphql(
    'query($owner: String!, $name: String!) {
      repository(owner: $owner, name: $name) { hasSponsorshipsEnabled }
    }',
    variables = list(owner = "trevorld", name = "ghcli")
  )
}
}
\seealso{
\url{https://cli.github.com/manual/gh_api}
}
